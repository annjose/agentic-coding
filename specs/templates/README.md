# Spec Templates

## New App
Use `app-spec-template.md` when building something from scratch.

## Feature Enhancement  
Use `feature-spec-template.md` when adding to existing code.

Replace `[placeholders]` with your specifics. Delete sections that don't apply.
